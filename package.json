{
  "name": "@iml/online-help",
  "version": "2.3.0",
  "description": "The IML help docs",
  "scripts": {
    "build":
      "rm -rf dist && bundle exec jekyll build --destination dist --baseurl '/help'",
    "start": "bundle exec jekyll serve",
    "postversion": "npm run build",
    "link-check":
      "find . -name '*.md' -not -path './node_modules/*' -not -path './_site/*' | xargs -n 1 -I % sh -c 'echo %; markdown-link-check %;'"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/intel-hpdd/Online-Help.git"
  },
  "author": "IML Team",
  "devDependencies": {
    "markdown-link-check": "^3.1.2"
  }
}
